import { storiesOf } from '@storybook/react';
import { StudentGroupsSectionbar } from '../../../website/src/components/StudentGroupsSectionbar';

storiesOf('StudentGroupsSectionbar', module).add('default', () => (
  <StudentGroupsSectionbar />
));
