query getBrandingPeriod($brandSlug: String!) {
  brandingPeriod(slug: $brandSlug) {
    id
    name
    logo {
      resource
    }
    logoVector {
      resource
    }
    description
    accent
    websiteLink
    bundleSet {
      slug
      name
    }
  }
}
