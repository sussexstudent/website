import React from 'react';
import { storiesOf } from '@storybook/react';
import CookieMessage from '~components/CookieMessage/index';

storiesOf('CookieMessage', module).add('default', () => <CookieMessage />);
