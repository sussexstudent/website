import initStoryshots from '@storybook/addon-storyshots';

/* TODO: add to each set before setup - to check for proptype warnings
console.error = jest.fn();
expect(console.error.mock.calls.length).toBe(0);
*/

initStoryshots();
