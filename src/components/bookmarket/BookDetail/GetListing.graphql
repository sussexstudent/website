query GetMarketListing($listingId: Int) {
  marketListing(listingId: $listingId) {
    pk
    bookTitle
    bookAuthor
    description
    buyPrice
    state
    section {
      title
      slug
    }
  }
}
