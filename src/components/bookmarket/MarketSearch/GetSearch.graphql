query GetMarketListing($filters: MarketListingsFilter) {
  allMarketListings(filters: $filters) {
    edges {
      node {
        pk
        bookTitle
        bookAuthor
        buyPrice
        description
      }
    }
  }
}
