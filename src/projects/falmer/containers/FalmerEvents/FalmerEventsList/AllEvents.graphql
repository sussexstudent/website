#import "../EventStuff.graphql"

query AllEvents($filter: EventFilter) {
  allEvents(filter: $filter) {
    edges {
      node {
        ...EventStuff
        children {
          ...EventStuff
        }
      }
    }
  }
}
