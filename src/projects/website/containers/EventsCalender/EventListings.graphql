query EventsListings($filter: EventFilterInput) {
  allEvents(filter: $filter) {
    edges {
      node {
        eventId
        slug
        title
        startTime
        endTime
        locationDisplay
        kicker
        shortDescription
        url
        cost
        ticketLevel
        ticketType
        bundle {
          name
        }
        venue {
          name
          websiteLink
        }
        featuredImage {
          resource
        }
      }
    }
  }
}
