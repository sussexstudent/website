query KbSection($sectionSlug: String) {
  knowledgeBase {
    section(slug: $sectionSlug) {
      slug
      title
      path
      ancestors {
        path
        title
      }
      topics {
        slug
        title
        articles {
          title
          slug
        }
      }
    }
  }
}
