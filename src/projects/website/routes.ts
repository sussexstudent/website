import { PreRouter } from '~libs/PreRouter';

export default new PreRouter([
  '/whats-on',
  '/sport-societies-media/discover',
  '/book-market',
  '/freshers',
  '/kb',
  '/search',
  /^\/get-involved$/,
  /^\/support$/,
]);
